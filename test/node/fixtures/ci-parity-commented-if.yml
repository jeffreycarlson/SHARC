name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Run full dist-based test suite
        # if: false
        run: npm run test:all:built
