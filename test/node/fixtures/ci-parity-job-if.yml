name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Run full dist-based test suite
        run: npm run test:all:built
