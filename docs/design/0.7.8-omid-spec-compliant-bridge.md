# 0.7.8 — OMID Spec-Compliant Bridge (`window.omid3p` Exposure, router-consuming)

**Status:** **REVISED draft — awaiting design review + Jeffrey's lock.** Not final, not ready-for-merge. This is the post-router revision of the parked draft: the 0.7.7 SHARC Protocol Router (#219 / PR #223) has **shipped** (released v0.7.7, `dist/sharc-protocol-router.mjs` live), so this design now *consumes* the router rather than re-inventing its envelope-gating and nonce-derivation. See § 0 for the diff-from-parked changelog.
**Target version:** 0.7.8
**Issue:** [#217](https://github.com/jeffreycarlson/SHARC/issues/217) (locked decision + acceptance criteria)
**Consumes (shipped prerequisite):** [#219](https://github.com/jeffreycarlson/SHARC/issues/219) — `SHARCProtocolRouter` primitive + renderer-protocol retrofit, **shipped in 0.7.7**. Design baseline: `docs/design/0.7.7-cross-frame-protocol-router.md`. This design is the router's **second consumer** (renderer protocol is the first).
**Lands with:** [#240](https://github.com/jeffreycarlson/SHARC/issues/240) — `placementSessionId` immutability tripwire + sanctioned re-mint API gated behind `rederiveAllProtocolNonces()` (RTR-D21). The sequential-impression discipline this design owes (§ 16 dep 4) is the first real consumer of per-impression re-derivation; the structural guard lands alongside it.
**Verified by (separate, not in scope):** [#244](https://github.com/jeffreycarlson/SHARC/issues/244) — Codex's validator integration: loads real DV/IAS/Moat/Integral vendor scripts against the corpus and asserts `omid3p` end-to-end. This design is the **producer**; #244 is the independent **verifier**. Do not fold validator design in here.
**Predecessors:** 0.7.3 (`docs/design/0.7.3-omid-wiring.md` — container-owned OMID lifecycle, D7/D17 removed the creative-side OMID API). 0.7.4 (`docs/design/0.7.4-omid-hardening.md` — `feature_load_failed` plumbing, baseUrl validation). 0.7.6 (sidecar `omidAutoInstall` auto-install path for declared OMID bids). 0.7.7 (`docs/design/0.7.7-cross-frame-protocol-router.md` — the router this design consumes).
**Related research:** `docs/research/OM-sdk-research.md`. `docs/research/SHARC-origin-context.md` § renderer trust boundary.
**Existing bridge:** `src/sharc-omid-bridge.js` (`OmidCompatBridge` — current container-owned-only model; no `window.omid3p` exposure).
**Reference implementations cited:** IAB Open-Measurement-JSClients `verification-client.js` (`getThirdPartyOmid()` and `VerificationClient` constructor). IAB OMID API v1.5 PDF. IAB OMID Web Implementation Guide v1.0. IAB AdCOM `OpenRTB support for OMSDK` (API framework code 7).

---

## Table of Contents

0. [What changed from the parked draft](#0-what-changed-from-the-parked-draft)
1. [Problem Statement](#1-problem-statement)
2. [Architecture Overview](#2-architecture-overview)
3. [Transport: OMID as a router consumer](#3-transport-omid-as-a-router-consumer)
4. [Exposure Timing & shim install](#4-exposure-timing--shim-install)
5. [`omid3p` Surface Contract (IAB-spec core)](#5-omid3p-surface-contract-iab-spec-core)
6. [Event Flow](#6-event-flow)
7. [Security: inherited from router + OMID-specific residue](#7-security-inherited-from-router--omid-specific-residue)
8. [`options.verificationScripts` Repositioning](#8-optionsverificationscripts-repositioning)
9. [The 7 router §16 hand-off dependencies — how this design satisfies each](#9-the-7-router-16-hand-off-dependencies)
10. [Test Strategy (producer-side only)](#10-test-strategy-producer-side-only)
11. [Migration Plan](#11-migration-plan)
12. [Implementation Plan](#12-implementation-plan)
13. [Open Questions & Design Risks](#13-open-questions--design-risks)
14. [Decision Log](#14-decision-log)

---

## 0. What changed from the parked draft

This revision rebases the parked pre-router draft (commit `6bbe69b`) onto the shipped 0.7.7 router. A reviewer who knows the parked draft should read this section first.

| Area | Parked draft | This revision | Why |
|------|--------------|---------------|-----|
| **Transport gating** | Bespoke per-OMID envelope validator (parked § 7.1) duplicating `_isValidRendererEnvelope`: hand-rolled `(source, origin, nonce, placementSessionId)` checks on `SHARC:Omid:Event` / `SHARC:Omid:Register`. | **Removed.** OMID registers `prefix: 'SHARC:Omid:'` with the router; the router's uniform gate (router § 3.2 steps 1–9) validates every inbound OMID envelope. The OMID handler does payload-shape + dispatch only. | Router § 16; #217 acceptance criterion "propagate via a SHARC Protocol Router protocol (prefix `SHARC:Omid:`)". |
| **Nonce derivation** | Bespoke: reuse the single `_sharcNonce` across renderer + OMID; parked § 7.6 checklist item "confirm `_sharcNonce` is acceptable to reuse." | **Removed.** The router derives a per-protocol OMID nonce (HMAC-SHA-256 over `_sharcNonce` salted with `placementSessionId`, router § 5.2) and delivers it via `onReady({protocolNonce})`. OMID never derives its own nonce and never reuses the renderer's. | Router § 5; resolves parked B1 (cross-channel nonce reuse) structurally. |
| **Nonce delivery to iframe** | Parked draft was hazy: "construction-time nonce" on envelopes, no spec'd non-markup channel. | The **router-derived OMID `protocolNonce`** authenticates the cross-frame `window.message` envelopes and lives in the publisher-page OMID extension. The iframe shim's outbound `SHARC:Omid:Register` envelopes are signed with the OMID `protocolNonce` injected into the shim via the trusted-injection mechanism (§ 4.3) — **never** via `location.hash`, **never** echoed to vendor JS. | Router § 16 deps 2 & 6; resolves parked B2 (nonce-delivery channel). |
| **Lifecycle phases** | Parked draft had no phase model — it predated the router's phase enumeration. | OMID message types are declared against the router's **reserved** `omid-active` / `omid-finishing` phases (router § 4.1, RTR-J1). No router code change — just `transitionTo` calls at the lifecycle points specified in § 3.4. | Router § 4.3 / RTR-J1; resolves parked S1 (cross-channel envelope-type impersonation) structurally via phase enforcement. |
| **Security sections** | Parked § 7.1 (transport trust anchors), § 7.4 (register-DoS), § 7.5 (srcdoc/opaque origin), § 7.6 checklist re-derived mitigations the router now owns. | **Collapsed.** § 7 now points to the router for B1/B2/S1 and the envelope/origin/nonce/phase anchors, and keeps only the OMID-specific residue: cross-vendor isolation (router § 16 dep 5), observer-amplification cap, faithful-relay discipline. | Router structurally owns the inherited mitigations; re-deriving them here would drift. |
| **§16 hand-off ledger** | Did not exist (the parked draft predates §16). | New § 9 walks all 7 router §16 OMID dependencies and states how this design satisfies each. | Router § 16 is the explicit hand-off contract. |
| **verificationScripts** | Already repositioned as supplementary, but framed loosely. | Tightened per #217's locked decision (§ 8): supplementary today; becomes **canonical** for future Native (`eventtrackers` event=555) and VAST (`<AdVerifications>`) sidecar conventions. | #217 locked decision. |
| **Version strings** | Said "NEW in 0.7.7" throughout (the parked draft pre-dated the release split). | Corrected to **0.7.8** throughout; the router is 0.7.7, this bridge is 0.7.8. | Release-split per #217 / router doc. |
| **Validator (Layer 2)** | Parked § 9.4 sketched Layer-2 validator as "out of scope, follow-up." | Explicitly handed to **#244 (Codex)** as the independent verifier; this doc keeps producer-only scope. | #244; producer/verifier split. |
| **`placementSessionId` immutability** | Not addressed. | Wired to **#240** (§ 9 dep 4): the sanctioned re-mint API + immutability guard land with this work. | #240; RTR-D21 tripwire. |

**Net:** the design is smaller. The router absorbed the transport, the nonce, and the gating; this doc is now about (a) IAB-spec `omid3p` correctness, (b) the publisher↔iframe OMID event relay expressed as a router protocol, and (c) the seven §16 obligations.

---

## 1. Problem Statement

0.7.3 locked OMID as a **container-only** measurement extension (D7 / D17): no creative-side API, no `window.omid3p` in the creative iframe. That decision matched the OM SDK Service Script / Session Client roles correctly for the **publisher-page side** of the integration but assumed the creative iframe would never host an OMID-aware verification script.

Three empirically-verified observations invalidate that assumption (see issue #217 evidence trail; corpus: 1,745 bids / 15 raw OpenRTB files, 422 declare AdCOM API 7):

1. **SHARC's current `OmidCompatBridge` exposes no `window.omid3p` anywhere.** `grep -rn "omid3p" src/` returns zero results in the production tree. `injectScripts(html)` and `injectIntoMarkup(html)` are explicit no-ops.

2. **The IAB OMID JS Verification Client expects `window.omid3p` in the same frame as the verification script.** The canonical detection code (`getThirdPartyOmid()` in IAB `Open-Measurement-JSClients` at `src/verification-client/verification-client.js`) reads `omidGlobal['omid3p']` from the resolved global context — it does **not** walk the frame tree, does **not** look at `window.parent` or `window.top`, and accepts only an object exposing two functions: `registerSessionObserver` and `addEventListener`. Verification scripts loaded inline in the creative iframe see no `omid3p` under SHARC today and fall back to non-OMID measurement modes.

3. **The corpus shows ~24% of API-7-declared bids (101 of 422) ship OMID-aware verification vendor scripts inline in the `adm`.** DoubleVerify's `cdn.doubleverify.com/dvtp_src.js` probes `t.omid3p` for `registerSessionObserver` capability, looks for `omweb-v1.js` in DOM, and accesses `omidVerificationProperties` — the canonical OMID JS Verification Client detection pattern. (For contrast: **0** corpus bids ship structured `verificationScripts` in `bid.ext.measurement.omid`, and **0** ship the canonical OMID Session Client in markup. The inline-`adm` path is the real-world OMID delivery channel.)

Separately, `OmidCompatBridge.options.verificationScripts` is a SHARC-invented config-time injection convention with **no IAB AdCOM/OpenRTB precedent for Banner or Video** (the AdCOM `OpenRTB support for OMSDK` doc defines a sidecar verification-script path only for Native, via `eventtrackers` with `event=555`). The path is still useful — publishers and wrappers do want to inject vendor scripts a creative did not bring inline — but it was positioned as the **canonical** delivery channel for HTML, and that framing is wrong.

**Scope of this design:** redesign the bridge to align with the IAB spec model — inline-loaded verification scripts find `window.omid3p` in the creative frame and register session observers — and express the publisher↔iframe event relay as a **router protocol** (prefix `SHARC:Omid:`), not bespoke machinery. `options.verificationScripts` is repositioned as supplementary (§ 8).

**SHARC-specific vs OMID-borrowed (acknowledgment, carried from router § 5.1).** The cross-frame back-channel between the publisher-page OMID extension and the creative-iframe shim is a **SHARC-specific** architectural feature. IAB OMID's canonical architecture has verification scripts running in container-loaded verification iframes with direct same-window `omid3p` access — no cross-frame channel, no nonce. The IAB OMID spec defines **no** nonce/token/signature/shared-secret mechanism; the AdSession identifier is a client-generated GUID and the trust model is structural same-realm co-location. SHARC needs a cross-frame transport that OMID's same-realm architecture never had, so SHARC's per-protocol nonce is solving a SHARC transport problem with standard crypto (the router's HMAC derivation), **not** implementing an IAB-spec requirement. This is engineering judgment, not a spec citation — and it must not leak into the IAB-facing `omid3p` surface (§ 9 dep 6).

---

## 2. Architecture Overview

### 2.1 Three-script OMID model — what runs where

```
┌────────────────────────────────────────────────────────────────────────────┐
│  Publisher Page (top window, container origin)                              │
│                                                                             │
│  ┌──────────────────────────────┐    ┌──────────────────────────────┐     │
│  │ OM SDK Service Script        │    │ SHARCContainer                │     │
│  │ (omweb-v1.js)                │    │  + OmidCompatBridge (extension)│    │
│  │ + Session Client             │    │    - owns AdSession lifecycle │     │
│  │ (omid-session-client-v1.js)  │    │    - registerSessionObserver  │     │
│  │ → window.OmidSessionClient   │◄───┤      on its own AdSession     │     │
│  └──────────────┬───────────────┘    │    - registers router protocol│     │
│                 │                    │      'SHARC:Omid:' on `load`  │     │
│                 │ OMID session events │    - relays events to iframe  │     │
│                 ▼                    └──────────────┬────────────────┘     │
│                                                     │                       │
│                                   container.protocolRouter ◄── owns the     │
│                                   (single `message` listener)   window.message│
│                                                     │            transport   │
│                                                     ▼                        │
│ ╔═══════════════════════════════════════════════════════════════════════╗ │
│ ║  Creative iframe (cross-origin renderer / cross-origin sandbox)        ║ │
│ ║                                                                        ║ │
│ ║  Loaded BEFORE document.write(creativeHtml):                           ║ │
│ ║   1. sharc-protocol.js   2. sharc-creative.js                          ║ │
│ ║   3. sharc-omid-shim.js  ◄── NEW in 0.7.8 → installs window.omid3p     ║ │
│ ║      (OMID protocolNonce baked in at injection; never location.hash)   ║ │
│ ║                                                                        ║ │
│ ║  Creative HTML (untrusted):                                            ║ │
│ ║   <script src="https://cdn.doubleverify.com/dvtp_src.js">              ║ │
│ ║     → reads window.omid3p                                              ║ │
│ ║     → omid3p.registerSessionObserver(observer, vendorKey, injId)       ║ │
│ ║     → omid3p.addEventListener('impression', cb, injId)                 ║ │
│ ╚═══════════════════════════════════════════════════════════════════════╝ │
└────────────────────────────────────────────────────────────────────────────┘
```

The IAB three-role model is preserved exactly:
- **Container-side Service Script** (`omweb-v1.js`) ↔ **Session Client** (`omid-session-client-v1.js`) on the publisher page — the integration partner's per-measurement-context asset. SHARC is the integration partner.
- **Vendor Verification Client** inline in the creative iframe — probes `window.omid3p`.
- The **publisher-page-owned `AdSession`** is the single source of truth, emitting `loaded()` / `impressionOccurred()` / lifecycle. The iframe shim is a one-way relay of those events to inline verification scripts.

### 2.2 What is new in 0.7.8

1. **`src/sharc-omid-shim.js`** — a new iframe-side shim that installs `window.omid3p` with the IAB-spec surface (two methods, plus internal event delivery). Loaded into the creative iframe via the same install point as the MRAID and SafeFrame bridges (§ 4).
2. **`OmidCompatBridge` becomes a router consumer.** From its `onContainerLifecycleEvent(event)` hook (on `event.type === 'load'`), it calls `event.container.protocolRouter.register({ prefix: 'SHARC:Omid:', types, handler, onReady })`. It registers a session observer on its own `AdSession`, serializes events, and relays them across the publisher↔iframe boundary **through the router's outbound helper** to subscribers registered through the iframe shim.
3. **`options.verificationScripts` semantics narrowed** (documentation + framing only; same field name, same shape) — supplementary publisher/wrapper/vendor injection, not the canonical HTML OMID delivery channel (§ 8).

### 2.3 What is unchanged

- Publisher-page OM SDK loading (`omSdkServiceScriptUrl`, `omSdkSessionClientUrl`). 0.7.3 § 3 stands.
- Container-owned `AdSession.start() / loaded() / impressionOccurred() / finish()` lifecycle. 0.7.3 § 4 stands.
- Feature name `com.iabtechlab.sharc.omid` in `supportedFeatures` when both SDK URLs are configured. 0.7.3 § 7 stands. AdCOM **API framework code 7** anchoring (per `project_adcom_anchor`) is unchanged — `creativeMeta.apis` carries code 7 for OMID-capable creatives.
- `KNOWN_BRIDGE_IDENTIFIERS` is **NOT** extended to include `'omid'` (§ 4.2) — OMID still does not route through the renderer-bridge resolver as an API-translation bridge.
- 0.7.6 `omidAutoInstall` sidecar normalization. The bid-metadata path still produces `options.verificationScripts` entries; those become supplementary per § 8.
- The router itself. This design adds **zero router-side code** (router § 16.2: "zero router-side adjustments needed"). OMID is purely a consumer: it `register()`s a prefix and calls `transitionTo` at the right lifecycle points.

---

## 3. Transport: OMID as a router consumer

This is the load-bearing change from the parked draft. The IAB spec dictates the API surface inside the iframe (`window.omid3p` with `registerSessionObserver` / `addEventListener` — § 5). It does **not** prescribe how a publisher-page-owned `AdSession` reaches that surface when the verification script is inline in a cross-origin creative iframe. The parked draft answered that with bespoke postMessage gating; this revision answers it with the router.

### 3.1 OMID is the router's second occupant

OMID's publisher↔iframe transport is a stream of `window.message` envelopes under the `SHARC:Omid:` prefix. The router owns `window.message` (router § 2.5: it does **not** own `MessageChannel` ports). Because the OMID event relay is `window.message` traffic, it goes through the router's single `message` listener and uniform gate (router § 3.2). The OMID extension does **not** add its own `window.addEventListener('message', ...)`.

> **Why router `window.message`, not a dedicated `MessageChannel` (router § 16 dep 2 alternative).** Router § 16 dep 2 notes a `MessageChannel`-delivered OMID nonce "does not interact with the router at all." That is an option, but it would put OMID transport *outside* the router's discipline and reintroduce the bespoke-gating problem the router exists to eliminate. **Decision OMID-D1:** OMID transport rides the router's `window.message` channel under `SHARC:Omid:`, so the router gate validates every inbound OMID envelope. `MessageChannel` is reserved for one thing only — the one-time **trusted injection** of the OMID `protocolNonce` into the shim at iframe creation (§ 4.3), which is exactly the router-§16-dep-2 "MessageChannel-transferred at iframe creation" mechanism. Steady-state OMID events ride the router channel; only the initial secret hand-off uses the port. This keeps the gate authoritative for all ongoing traffic.

### 3.2 Registration shape

`OmidCompatBridge.onContainerLifecycleEvent(event)` fires on `event.type === 'load'` (after the renderer protocol is already registered by the container itself — router § 2.3 ordering). It calls:

```javascript
// In OmidCompatBridge, from onContainerLifecycleEvent({ type: 'load', container }):
container.protocolRouter.register({
  prefix: 'SHARC:Omid:',
  types: {
    // Inbound: iframe shim → publisher. Subscription handshake. Valid once the
    // shim exists (post-render) through the active session.
    'Register':   { phases: ['rendered', 'creative-active', 'omid-active'], direction: 'inbound' },
    'Unregister': { phases: ['rendered', 'creative-active', 'omid-active'], direction: 'inbound' },
    // Outbound: publisher → iframe. Session lifecycle + metric events.
    // Session control (start/finish) valid across the OMID-active window.
    'Event':      { phases: ['omid-active', 'omid-finishing'], direction: 'outbound' },
  },
  handler: this._handleOmidEnvelope.bind(this),   // payload-shape + dispatch ONLY; gate already done
  onReady: ({ protocolNonce }) => {
    // Router-derived OMID per-protocol nonce. Stash for outbound construction
    // and for trusted injection into the shim (§ 4.3). Never echoed to vendor JS.
    this._omidProtocolNonce = protocolNonce;
  },
});
```

The router validates `(source, origin, prefix, type-membership, placementSessionId, OMID protocolNonce, phase-membership)` on every inbound `SHARC:Omid:Register` / `:Unregister` envelope before `_handleOmidEnvelope` ever runs (router § 3.2 steps 1–9). The handler is responsible **only** for payload-shape validation (`subscription.kind`, `injectionId` present, etc.) and dispatch into the subscription registry.

> **Open question OMID-Q1 (phase membership for `Register`).** Per OMID's "call upon initialization" semantics, a verification script may call `registerSessionObserver` *before* `AdSession.start()` (the script runs as soon as the creative HTML executes; the publisher-page session may still be loading). The shim queues such early calls locally (§ 5.5) and only posts `SHARC:Omid:Register` to the publisher once a subscription is materialized — but if the shim posts at queue time, the inbound envelope can arrive while the router is still in `rendered`/`creative-active` (pre-`omid-active`). The `types` map above declares `Register` valid in `rendered` + `creative-active` + `omid-active` to cover this. **Risk:** if the shim instead defers posting until `sessionStart`, `Register` need only be valid in `omid-active`, tightening the phase gate. The narrower gate is preferable for defense-in-depth but depends on the shim's queue-vs-post timing (§ 5.5). Resolve when § 5.5's queueing policy is locked — do not guess; the two are coupled.

### 3.3 Outbound envelopes go through `buildOutbound`

The publisher-side relay constructs `SHARC:Omid:Event` envelopes via the router helper (router § 3.3), which stamps the OMID `protocolNonce` and `placementSessionId` and asserts the type is declared `outbound`:

```javascript
const envelope = container.protocolRouter.buildOutbound('SHARC:Omid:', 'Event', {
  sequence: this._nextOmidSequence(),  // monotonic uint32, per session — OMID-payload field
  event: {
    adSessionId: this._adSessionId,    // OM SDK session id, read off the publisher AdSession
    timestamp: Date.now(),
    type: 'sessionStart',              // EventType per § 5.3
    data: { /* event-type-specific payload */ },
  },
});
// → { type: 'SHARC:Omid:Event', sharcNonce: <OMID protocolNonce>,
//     placementSessionId: <container.placementSessionId>, sequence, event }
this._container._iframe.contentWindow.postMessage(envelope, this._iframeOrigin);
```

`buildOutbound` throws synchronously if `sequence`/`event` collide with a router-controlled field (`type`/`sharcNonce`/`placementSessionId`) — they don't, so this is a programming-error tripwire, not a runtime concern (router § 3.3 / RTR-D2).

The iframe shim's **inbound** `SHARC:Omid:Register` envelopes are constructed the same way on the shim side, signed with the OMID `protocolNonce` the shim received via trusted injection (§ 4.3). The shim does not have access to `buildOutbound` (that's a publisher-page helper), so the shim constructs the envelope shape by hand but uses the injected nonce verbatim.

### 3.4 Phase transitions — the OMID lifecycle points

OMID declares its types against the router's **reserved** `omid-active` / `omid-finishing` phases (router § 4.1 — both reserved at RTR-J1 for exactly this consumer). No router code changes; the OMID extension drives `transitionTo` at these points:

| Phase entered | Call site (in `OmidCompatBridge`) | Trigger | Exit |
|---------------|-----------------------------------|---------|------|
| `omid-active` | Immediately after `AdSession.start()` resolves successfully (the existing `_createSession()` success path) | Publisher-page OMID session has started. This is router § 4.1's documented `omid-active` entry condition ("`AdSession.start()` returned successfully"). | → `omid-finishing` on `finish()`, or `terminated` on container teardown |
| `omid-finishing` | At the top of the `AdSession.finish()` path, before `_resetSessionRefs(true)` | Session is winding down; covers OMID v1.5's ~1.0s `sessionFinish` grace window during which `sessionFinish` may still need to deliver to observers | → `terminated` (container `_terminate()` already calls `transitionTo('terminated')` per router § 4.2) |

> **Interaction with `creative-active`.** The router's `creative-active` phase (router § 4.1) is the container's steady state and is entered by the container itself, independent of OMID. OMID's `omid-active` is a *narrower* window that opens *after* `AdSession.start()`, which itself happens after the container is live. So the real-world phase sequence is: `attaching-renderer` → `rendered` → `creative-active` → (`AdSession.start()`) → `omid-active` → (`finish()`) → `omid-finishing` → `terminated`. **Design note:** the router models phases as an open string set with one "current" phase (router § 4.3). `omid-active` *replaces* `creative-active` as the current phase when OMID starts. That is why `SHARC:Omid:Register` is declared valid in `rendered` + `creative-active` + `omid-active` (a verification script can register across that whole span), while `SHARC:Omid:Event` is valid only in `omid-active` + `omid-finishing` (events only flow once the session exists). See OMID-Q1 for the open edge.

> **Open question OMID-Q2 (does OMID own `transitionTo`?).** Router § 7.4 / RTR-D14: `transitionTo()` is called only from **container-internal** code paths and is **not** exposed to extensions. But `OmidCompatBridge` is an extension and needs to enter `omid-active` / `omid-finishing`. Two resolutions: (a) the container exposes a *narrow, OMID-reserved* `transitionTo` affordance to the OMID extension specifically (widens the router's trust surface slightly); or (b) the container itself drives `omid-active` / `omid-finishing` transitions by observing the OMID extension's session lifecycle (keeps `transitionTo` container-internal, but couples the container to OMID's AdSession state). **Recommendation:** (b) — keep `transitionTo` container-internal per RTR-D14; have the OMID extension emit a container-internal signal (e.g. the existing extension→container lifecycle channel) that the container maps to the phase transition. This preserves the router's "phases are container-controlled" invariant (router § 7.5 trust-anchor table). **This needs a Security Engineer review pass** — it touches the phase-transition trust anchor. Flagged for Jeffrey's lock.

### 3.5 Envelope shapes (router-conformant)

Inbound (iframe shim → publisher), validated by the router gate:

```javascript
{
  type: 'SHARC:Omid:Register',
  sharcNonce: '<OMID protocolNonce — router gate step 7 validates>',
  placementSessionId: '<router gate step 6 validates>',
  subscription: {
    kind: 'sessionObserver' | 'eventListener',
    vendorKey: '<string, optional for sessionObserver, absent for eventListener>',
    eventType: '<string, present iff kind === "eventListener">',
    injectionId: '<string, verification-client injectionId>',
    subscriptionId: '<opaque shim-side id, scopes future Unregister>'
  }
}
```

Outbound (publisher → iframe shim), built by `buildOutbound`:

```javascript
{
  type: 'SHARC:Omid:Event',
  sharcNonce: '<OMID protocolNonce>',
  placementSessionId: '<container.placementSessionId>',
  sequence: <monotonic uint32, per session>,
  event: { adSessionId, timestamp, type, data }
}
```

The **iframe side** of the gate (the shim validating *inbound* `SHARC:Omid:Event` envelopes from the publisher) is **not** the router — the router lives on the publisher page. The shim runs its own minimal inbound check: `event.source === window.parent`, `event.origin === <recorded container origin>`, `event.data.sharcNonce === <injected OMID protocolNonce>`, `event.data.placementSessionId === <recorded>`. This mirrors the publisher-side gate's anchors but is shim-local code (the router is a publisher-page primitive; there is no router instance inside the creative iframe). See OMID-Q3.

> **Open question OMID-Q3 (shim-side inbound validation).** The router's discipline lives on the publisher page; the iframe shim must validate publisher→iframe envelopes with its own code. This is a small, OMID-specific validator (4 checks) — *not* a re-implementation of the router (the shim has no concept of phases, prefixes-registry, or multiple protocols; it knows exactly one nonce and one counterpart). **Risk:** drift between the shim's 4-check validator and the router's anchor set. **Mitigation:** § 10 pins a test asserting the shim rejects bad-source / bad-origin / bad-nonce / bad-placementSessionId inbound envelopes. Acceptable residue — the alternative (shipping a router instance into the hostile iframe) is strictly worse.

---

## 4. Exposure Timing & shim install

### 4.1 Install `window.omid3p` before `document.write(creativeHtml)`

Issue #217: "working assumption for `omid3p` exposure timing parallels existing MRAID / SafeFrame / SHARC bridge attachment." The renderer (Creative Markup variant) loads compatibility bridge modules from `data.bridges` **before** `document.write(creativeHtml)` — the same install point as `window.mraid` and `window.$sf.ext`. The 0.7.8 OMID shim installs at this point so the verification script's first synchronous read of `window.omid3p` succeeds (OMID `isSupported()` contract — § 9 dep 1).

### 4.2 `'omid'` stays out of `KNOWN_BRIDGE_IDENTIFIERS`

The 0.7.3 rationale stands — OMID is not an API-translation bridge; `KNOWN_BRIDGE_IDENTIFIERS` is reserved for creative-facing compat shims (MRAID, SafeFrame). Instead the renderer loads `sharc-omid-shim.js` as a separate, OMID-specific install:

- **Creative Markup variant (renderer):** the renderer accepts an `omid` boolean on the `SHARC:Renderer:render` envelope (which now flows through the router's `buildOutbound('SHARC:Renderer:', 'render', {...})` post-0.7.7-retrofit). When set, the renderer loads `sharc-omid-shim.js` from its same-origin allowlist (same security model as `loadBridgeModule`) **before** `document.write(creativeHtml)`.
- **Creative URL variant (`useMarkupInjection`):** `OmidCompatBridge.injectIntoMarkup(html)` — currently a no-op — prepends `<script src="<sharc base>/sharc-omid-shim.js"></script>` before any creative content, only when `omid3p` exposure is enabled (§ 12.3).
- **Creative URL variant (direct `iframe.src`, no markup access):** **not supported.** SHARC cannot inject a script into a frame whose markup it does not own; the container does not advertise `com.iabtechlab.sharc.omid`. Operator opt-in lever: `useMarkupInjection: true`. (Decision OMID-D11, carried.)

### 4.3 Trusted nonce injection into the shim (router § 16 dep 2)

The OMID `protocolNonce` (router-derived, delivered to the publisher-page extension via `onReady`) must reach the iframe shim **without** being creative-readable — **never** via `location.hash`, query string, or DOM attribute. Two viable mechanisms (router § 16 dep 2 leaves the choice to this design):

| Mechanism | How | Tradeoff |
|-----------|-----|----------|
| **(i) Renderer source-rewrite** | The renderer rewrites `sharc-omid-shim.js`'s source with the OMID `protocolNonce` baked into a closure constant *before* `document.write`, the same way the renderer pre-rewrites other injected modules. | Matches the renderer's existing module-injection path. Nonce never transits a creative-readable surface. Requires the renderer to know the OMID nonce at render time. |
| **(ii) `MessageChannel`-transferred at iframe creation** | At iframe creation the publisher posts the OMID `protocolNonce` over a one-time `MessageChannel` port (transferred, not broadcast). The shim's port-receive handler stashes it on private closure state. | Router § 16 dep 2's named alternative. The `MessageChannel` does **not** interact with the router (router owns `window.message`, not ports — RTR-D8). Slightly more iframe-side code. |

**Decision OMID-D2 (recommend, not locked):** prefer **(i)** for the Creative Markup (renderer) variant — it reuses the renderer's existing pre-rewrite path and keeps the nonce out of any post-load message entirely. Use **(ii)** for the Creative URL + `useMarkupInjection` variant, where the renderer isn't in the loop and the container injects the shim via `injectIntoMarkup`; the container holds the port. Both keep the nonce off `location.hash`. **Open question OMID-Q4:** whether the source-rewrite path (i) is compatible with `srcdoc` injection in the URL variant (the rewrite happens in the renderer; the URL variant has no renderer). If (i) can't serve the URL variant, (ii) is the only option there — confirm during implementation; do not assume the renderer path generalizes.

The OMID `protocolNonce` and the renderer-protocol nonce are **different** per-protocol nonces (router § 5.2: each prefix derives independently). Leaking the OMID nonce does not let the creative forge renderer envelopes (router § 7.1, layer 1). Leaking the renderer nonce — which never enters the iframe — is structurally prevented.

---

## 5. `omid3p` Surface Contract (IAB-spec core)

This is the whole point of 0.7.8 and is **not** weakened by the router consumption. Verified against IAB `Open-Measurement-JSClients` at `src/verification-client/verification-client.js`.

### 5.1 Public API (the contract verification scripts depend on)

`getThirdPartyOmid()` accepts an object iff it exposes both methods as functions:

```javascript
window.omid3p = {
  /**
   * Register a session-wide observer that receives ALL session events.
   * @param {function(event)} observer - receives { adSessionId, timestamp, type, data }
   * @param {string=} vendorKey - optional vendor identifier
   * @param {string} injectionId - opaque id supplied by the verification client
   */
  registerSessionObserver: function (observer, vendorKey, injectionId) { ... },

  /**
   * Register a single-event-type listener.
   * @param {string} eventType - one of the IAB OMID event-type strings (§ 5.3)
   * @param {function(event)} listener - receives { adSessionId, timestamp, type, data }
   * @param {string} injectionId - opaque id supplied by the verification client
   */
  addEventListener: function (eventType, listener, injectionId) { ... }
};
```

**Nothing else is on the contract.** SHARC must not expose additional methods on `window.omid3p`. Verification scripts probe `omid3p && typeof omid3p['registerSessionObserver'] === 'function' && typeof omid3p['addEventListener'] === 'function'` (the OMID `isSupported()` contract — § 9 dep 1). Adding surface invites coupling to SHARC-specific APIs that will not survive OMID spec revisions. **Critically: the OMID `protocolNonce` is NOT on this surface and never reaches any observer callback, return value, or payload (§ 9 dep 6).**

### 5.2 Event shape delivered to observers

```javascript
{
  adSessionId: '<OM SDK session id from publisher-page AdSession>',
  timestamp:   <ms since epoch — publisher-side wall clock>,
  type:        '<string, see § 5.3>',
  data:        { /* event-type-specific payload, may be empty {} */ }
}
```

`adSessionId` is the OM SDK Session Client's session id read off the publisher-page `AdSession` after `start()`; reused for every event in the session, matching the OMID spec. **The envelope's `sharcNonce` / `placementSessionId` / `sequence` transport fields are stripped by the shim — they never appear in the observer event** (§ 9 dep 6).

### 5.3 Event types supported in 0.7.8

Subset matching what `OmidCompatBridge` already fires on the publisher-page `AdSession`:

| EventType | When fired | Source on publisher page |
|-----------|-----------|--------------------------|
| `sessionStart` | After `AdSession.start()` succeeds | `registerSessionObserver` callback `type==='sessionStart'` |
| `sessionFinish` | After `AdSession.finish()` | `registerSessionObserver` callback `type==='sessionFinish'` |
| `sessionError` | OM SDK reports a session error | `registerSessionObserver` callback `type==='sessionError'` |
| `loaded` | After `AdEvents.loaded(...)` | Synthesized by bridge at `_fireLoaded()` |
| `impression` | After `AdEvents.impressionOccurred()` | Synthesized by bridge at `_fireImpression()` |
| `geometryChange` | On viewability/placement change | Synthesized at `_signalVisibility` / `_handlePlacementChange` |

**Deferred to follow-up issues:** video-specific event types (`start`, `firstQuartile`, `midpoint`, `thirdQuartile`, `complete`, `pause`, `resume`, `bufferStart`, `bufferFinish`, `skipped`, `volumeChange`, `playerStateChange`, `adUserInteraction`). 0.7.3 § 8.1 already lists these as future work; the shim mirrors that coverage.

`addEventListener` accepts any event-type string; if the shim hasn't been wired to fire that type, the listener is registered and silently receives nothing. **No `OMID_UNSUPPORTED_EVENT_TYPE` error** — verification scripts probe broadly and rely on absence-of-event semantics.

### 5.4 Observer invocation rules

- `registerSessionObserver` observers receive every event in the active session, in delivery order (matches the IAB OMID spec).
- `addEventListener(eventType, ...)` listeners receive only matching-`type` events.
- A verification script that registers an observer **after** `sessionStart` has fired gets the most-recent `sessionStart` replayed synchronously, then live events. Matches the spec intent that observers see a coherent session timeline regardless of registration order.
- `loaded` / `impression` are session-singletons (already guarded by `OmidCompatBridge._fireLoaded`/`_fireImpression`).

> **Open question OMID-Q5 (replay policy — beyond spec letter).** The OMID spec describes the API but not the ordering guarantee for late-registered observers. The replay-most-recent-`sessionStart` policy is a judgment call. Recommend it as durable default (cap of 1 replayed event keeps the surface bounded — § 7.3). Jeffrey-decision flag.

### 5.5 Out-of-session calls & response correlation

If a verification script calls `registerSessionObserver` / `addEventListener` before `OmidCompatBridge` has created its `AdSession` (publisher-page OM SDK still loading), the shim queues the subscription locally and materializes it when the session starts. The shim correlates the publisher's acknowledgment of a registration to the originating call via a **GUID-keyed `callbackMap`** (router § 16 dep 7 / OMID's own internal architecture): each `registerSessionObserver` mints a GUID, stored in `callbackMap[guid] = observer`, sent as `subscription.subscriptionId`; the publisher's responses (and any future `Unregister` acks) carry that GUID so the shim resolves the right local callback. See § 9 dep 7.

> **Open question OMID-Q1 cross-ref:** the queue-vs-post timing here determines the `Register` phase membership in § 3.2. Lock them together.

---

## 6. Event Flow

### 6.1 End-to-end sequence

```mermaid
sequenceDiagram
    participant Pub as Publisher Page<br/>(OmidCompatBridge + router)
    participant OMSDK as OM SDK<br/>(omweb-v1.js + session-client)
    participant Iframe as Creative iframe<br/>(sharc-omid-shim.js)
    participant Vscript as Verification Script<br/>(e.g. dvtp_src.js)

    Note over Pub: container construction → router constructed,<br/>renderer protocol registered first
    Note over Pub: onContainerLifecycleEvent('load')
    Pub->>Pub: protocolRouter.register({prefix:'SHARC:Omid:', ...})
    Pub-->>Pub: onReady({protocolNonce}) → stash OMID nonce
    Pub->>OMSDK: load omweb-v1.js + omid-session-client-v1.js
    Note over Pub: container.load()
    Pub->>Iframe: create iframe; inject shim with OMID nonce<br/>(trusted injection, §4.3 — not location.hash)
    Iframe->>Iframe: sharc-omid-shim.js installs window.omid3p<br/>+ inbound listener for SHARC:Omid:Event

    Note over Iframe: creative HTML executes
    Vscript->>Iframe: read window.omid3p (isSupported() → true)
    Vscript->>Iframe: omid3p.registerSessionObserver(cb,'doubleverify',injId)
    Iframe->>Iframe: callbackMap[guid]=cb (response correlation)
    Iframe->>Pub: postMessage SHARC:Omid:Register<br/>(signed w/ OMID nonce; router gate validates)

    Note over Pub: AdSession.start() succeeds
    Pub->>Pub: protocolRouter.transitionTo('omid-active')
    OMSDK-->>Pub: sessionStart (via registerSessionObserver)
    Pub->>Iframe: buildOutbound('SHARC:Omid:','Event',{type:'sessionStart'})
    Iframe->>Vscript: cb({adSessionId, type:'sessionStart', ...})

    Pub->>OMSDK: AdEvents.loaded(...)
    Pub->>Iframe: SHARC:Omid:Event {type:'loaded'}
    Iframe->>Vscript: cb({type:'loaded', ...})
    Pub->>OMSDK: AdEvents.impressionOccurred()
    Pub->>Iframe: SHARC:Omid:Event {type:'impression'}
    Iframe->>Vscript: cb({type:'impression', ...})

    Note over Pub: container teardown
    Pub->>Pub: protocolRouter.transitionTo('omid-finishing')
    Pub->>OMSDK: AdSession.finish()
    OMSDK-->>Pub: sessionFinish
    Pub->>Iframe: SHARC:Omid:Event {type:'sessionFinish'}
    Iframe->>Vscript: cb({type:'sessionFinish', ...})
    Iframe->>Iframe: drop window.omid3p (refuse new subscriptions)
    Note over Pub: _terminate() → transitionTo('terminated')
```

### 6.2 Where the publisher-page bridge taps into OM SDK

`OmidCompatBridge._createSession()` already calls `self._omid.adSession.registerSessionObserver(...)` (`src/sharc-omid-bridge.js` L865–871). 0.7.8 extends that observer to:

1. On every event from OM SDK, build the envelope via `buildOutbound('SHARC:Omid:', 'Event', {...})` and post it to the iframe's `contentWindow`.
2. Track the iframe origin captured at iframe creation (same value as `this._rendererOrigin` for the Markup variant; for the URL `srcdoc` variant the origin may be `null`/opaque — § 7.4).
3. Drive `transitionTo('omid-active')` on `start()` success and `transitionTo('omid-finishing')` on `finish()` (subject to OMID-Q2).
4. Continue the existing `sessionFinish` → `_resetSessionRefs(true)` cleanup plus a final teardown envelope.

For `loaded` / `impression` / `geometryChange` (synthesized by the bridge, not emitted by OM SDK), the bridge posts the envelope at the same call sites where it currently calls `_fireLoaded` / `_fireImpression` / `_signalVisibility`.

### 6.3 Events flow one way

The IAB `omid3p` surface is **observer-only**. Verification scripts subscribe; they do not emit. There is no `omid3p.publishEvent()` — vendor measurement signals flow vendor-script → vendor-CDN over the script's own HTTP fetch, not back through `omid3p`. So the OMID router protocol is **unidirectional** for events (`Event` is `outbound` only) and bidirectional only for the registration handshake (`Register`/`Unregister` are `inbound`). The `types` map in § 3.2 encodes exactly this asymmetry.

---

## 7. Security: inherited from router + OMID-specific residue

### 7.1 Inherited from the router (do not re-derive here)

The parked draft's bespoke transport-security sections are **structurally resolved** by the router. Pointers, not re-derivations:

| Parked finding | Resolution | Router reference |
|----------------|------------|------------------|
| **B1 — cross-channel nonce reuse** (renderer + OMID sharing `_sharcNonce`) | Eliminated. The router derives a **distinct** per-protocol nonce for `SHARC:Omid:` (HMAC over `_sharcNonce` salted with `placementSessionId`). Leaking the OMID nonce does not yield the renderer nonce (non-invertible derivation). | Router § 5.2, § 5.4, § 7.1 layer 1 |
| **B2 — nonce-delivery channel** (how does the iframe get a nonce without the creative reading it?) | The router delivers the OMID `protocolNonce` to the **publisher-page** extension via `onReady`; the extension injects it into the shim via trusted injection (§ 4.3), never via `location.hash`. | Router § 5.3, § 16 dep 2 |
| **S1 — cross-channel envelope-type impersonation** (creative forges `SHARC:Renderer:rendered` from the iframe) | Eliminated structurally by (a) per-protocol nonces — the creative can't get the renderer nonce — and (b) phase enforcement — `SHARC:Renderer:rendered` is only valid in `attaching-renderer`, long past by the time any shim runs. | Router § 7.1 (both layers), § 3.2 step 9 |
| Transport trust anchors (source / origin / nonce / placementSessionId / type-prefix / phase) | The router's uniform gate (steps 1–9) validates every inbound `SHARC:Omid:` envelope before `_handleOmidEnvelope`. | Router § 3.2, § 7.5 |
| Register-DoS (creative spams `SHARC:Omid:Register`) | Inbound envelopes hit the router gate first (cheap, O(1)); a malformed or wrong-nonce envelope is silent-dropped at the gate. The OMID handler additionally caps materialized subscriptions (§ 7.3). The router does **not** rate-limit (router § 2.5) — protocol-specific caps remain OMID's job. | Router § 2.5, § 3.2 |

The **only** new-to-OMID gate the router does *not* provide is the **shim-side inbound validator** (§ 3.5 / OMID-Q3) — because the router is a publisher-page primitive and there is no router instance inside the hostile iframe. That validator is 4 checks, pinned by test (§ 10).

### 7.2 OMID-specific residue: cross-vendor isolation (router § 16 dep 5)

The router correctly draws the boundary at "one protocol = one nonce + one handler" (router § 16 dep 5). **Cross-vendor isolation inside the creative iframe is 0.7.8's responsibility** — the router cannot help, because it never sees inside the iframe.

The threat: multiple vendor verification scripts run in the same creative-iframe JS context. The shim must dispatch each session event to each registered observer **without** letting a hostile vendor script harvest another vendor's events.

- **Canonical (OMID same-window dispatch):** the shim invokes each registered observer's callback **directly** in the same JS context (`observer(event)`), exactly as OM SDK's own same-window model does. This is **not** a cross-frame call and **not** a broadcast.
- **Forbidden:** `iframe.contentWindow.postMessage(...)` for shim→vendor delivery. A broadcast `postMessage` is visible to *every* `message` listener in the iframe — including a hostile vendor script that subscribed to harvest competitors' events. The shim must use direct callback invocation, never in-iframe broadcast.
- **Per-vendor `MessageChannel` (edge case):** if a vendor's verification script ever lives in its *own* nested iframe (uncommon for OMID v1.5 in-shim integration), a dedicated port per vendor avoids the broadcast leak. Out of scope for the common case; noted for completeness.

> **Decision OMID-D5 (locked, carried):** the shim is a **faithful relay**, not an information channel. The `data` payload mirrors exactly what OM SDK delivers to a same-frame OMID integration — no SHARC-specific or publisher-page-specific fields. Hostile creative code observing events is an accepted OMID-spec threat (the creative author chose which verification scripts to inline). What SHARC must not do is *add* sensitive data to the relay.

### 7.3 OMID-specific residue: observer-amplification cap

A hostile creative could register thousands of observers and trigger a high-frequency event (`geometryChange` on resize churn) for `O(observers × events)` amplification.

- The shim caps total subscriptions per iframe at **`MAX_OMID_SUBSCRIPTIONS = 64`**. Calls past the cap are accepted silently and ignored (same surface as a vendor SDK that ignores registration internally).
- The shim caps replay to a late registrant at **1** (only the most recent `sessionStart`).
- The publisher-page bridge rate-limits `geometryChange` to ≤1 event / 100ms (matches OM SDK Service Script's internal throttle).

> **Decision OMID-D6 (architectural judgment, recommend lock):** caps at 64. Conservative; no real-world OMID measurement context registers more than a handful of observers. Should be tunable via constructor option. Jeffrey may have field intelligence justifying a different number.

### 7.4 OMID-specific residue: srcdoc / opaque-origin shim-side validation

For the Creative URL + `useMarkupInjection` variant, the iframe is `srcdoc`. Chromium srcdoc iframes inherit the parent origin; Safari and others may give `null` / opaque origin. The **shim-side** inbound validator (§ 3.5) cannot rely on `event.origin` alone.

**Mitigation:** the shim's inbound check treats `event.source === window.parent` as load-bearing and `event.origin` as best-effort: pass if origin is non-null and matches the recorded origin; pass if origin is `null`/opaque and `event.source === window.parent`; fail otherwise. The injected OMID `protocolNonce` is the second-layer defense. On the **publisher side**, the router gate's step 2 (`event.source === iframe.contentWindow`) is unaffected by opaque origin — `contentWindow` identity is browser-set and unforgeable (router § 7.5). The opaque-origin concern is therefore confined to the shim's own validator.

> **Decision OMID-D9 (architectural judgment, security implications):** best-effort origin + load-bearing `event.source` for the shim-side validator. **Needs Security Engineer review pass.** Note this is *narrower* than the parked draft framed it: the publisher-side gate is the router's and is opaque-origin-safe via `contentWindow` identity; only the shim's own inbound validator deals with opaque origin.

### 7.5 Hardening checklist (Code Reviewer + Security Engineer pass)

- [ ] OMID registers `prefix: 'SHARC:Omid:'` with correct `types`/`phases`/`direction`; no bespoke `window.addEventListener('message', ...)` in the OMID extension.
- [ ] Outbound envelopes built via `buildOutbound`; no hand-stamped `sharcNonce`/`placementSessionId` on the publisher side.
- [ ] OMID `protocolNonce` never appears in any observer callback, return value, or `data` payload (§ 9 dep 6 — pin a test).
- [ ] Shim-side inbound validator: 4 checks (source/origin/nonce/placementSessionId); rejects each failure (§ 3.5 / OMID-Q3 test).
- [ ] Cross-vendor delivery is direct callback invocation, never in-iframe `postMessage` broadcast (§ 7.2).
- [ ] `MAX_OMID_SUBSCRIPTIONS` cap enforced on the shim side.
- [ ] Phase transitions (`omid-active` / `omid-finishing`) driven only via the OMID-Q2-resolved path (container-internal, not extension-exposed `transitionTo`).
- [ ] Logging discipline: no envelope payload contents in logs (counts + rejection reasons only).

---

## 8. `options.verificationScripts` Repositioning

### 8.1 Same shape, narrowed semantics (per #217's locked decision)

The field stays. Its shape stays. Its semantics narrow — it is no longer the canonical OMID delivery channel for HTML; it is **supplementary publisher / wrapper / vendor injection**: a path for a publisher page, prebid wrapper, container host, or specific vendor to inject *additional* verification scripts **beyond what the creative ships inline**.

| Aspect | Before | After |
|--------|--------|-------|
| Field name | `OmidCompatBridge.options.verificationScripts` | unchanged |
| Field shape | `Array<{ url \| resourceUrl, vendor, accessMode, verificationParameters }>` | unchanged |
| Behavior on publisher page | Passed to `Context(partner, verificationScripts)` so OM SDK injects them into `OmidVerificationProperties` for verification scripts in its own measurement iframe | unchanged |
| Behavior in creative iframe | (None — `injectIntoMarkup` was a no-op for OMID) | Still none — the shim does **not** consume `options.verificationScripts` |
| Doc framing | "canonical OMID delivery channel" | **Supplementary** path for publisher/wrapper/vendor injection. **Canonical** HTML OMID delivery is `window.omid3p` exposure in the creative iframe (§ 5). This path **becomes canonical** when SHARC adds Native (`eventtrackers` event=555) and VAST (`<AdVerifications>`) creative formats — until then, supplementary. |

### 8.2 Duplicate-registration is accepted by design

A creative may ship an inline `<script src="dvtp_src.js">` *and* the publisher may pass the same vendor in `options.verificationScripts`. Both reach OM SDK, but via different mechanisms (inline → `omid3p.registerSessionObserver` in the iframe shim; publisher-injected → OM SDK's own measurement context via `OmidVerificationProperties`). These are two separate executions in two separate measurement contexts — the IAB OMID model accepts this (each context is its own session view). SHARC does **not** deduplicate (cannot — the publisher-injected version runs in a different frame). No detection, no warning, no error (Decision OMID-D12, carried).

### 8.3 No field rename, no deprecation

Pre-1.0 convention: same field name, no alias, no warning, no migration period. `validateVerificationScripts` rules unchanged. The narrowing is documentation-only.

---

## 9. The 7 router §16 hand-off dependencies

Router § 16 enumerated seven items the 0.7.8 OMID implementation MUST deliver. Each is walked here with the satisfying design.

| # | Router §16 dependency | How this design satisfies it | Where |
|---|------------------------|------------------------------|-------|
| **1** | **Shim install ordering.** `window.omid3p` exposed synchronously before verification scripts run; iframe methods return sync after queueing locally. (`isSupported()`: `omid3p && registerSessionObserver && addEventListener` are functions.) | Shim installs at the bridge-injection point **before** `document.write(creativeHtml)` (§ 4.1). `registerSessionObserver` / `addEventListener` queue locally (§ 5.5) and return synchronously. `window.omid3p` exposes exactly the two probed methods (§ 5.1). | § 4.1, § 5.1, § 5.5 |
| **2** | **Trusted nonce injection** (not `location.hash`; renderer source-rewrite or `MessageChannel`-transfer at iframe creation). | OMID `protocolNonce` (router-derived, delivered to publisher extension via `onReady`) injected into the shim via renderer source-rewrite (Markup variant) or `MessageChannel` transfer (URL+srcdoc variant), never `location.hash` (§ 4.3). The `MessageChannel` does not interact with the router (RTR-D8). Steady-state events ride the router channel (§ 3.1, OMID-D1). | § 3.1, § 4.3 |
| **3** | **Phase-membership declarations.** Registration types valid `rendered`/`creative-active` (pre-`start()`); metric events gated to `omid-active`; optional `omid-finishing` for v1.5's grace window. | `types` map declares `Register`/`Unregister` valid in `rendered` + `creative-active` + `omid-active`; `Event` valid in `omid-active` + `omid-finishing` (§ 3.2). `transitionTo('omid-active')` on `start()` success; `transitionTo('omid-finishing')` on `finish()` (§ 3.4). Uses the router's **reserved** phases — zero router code change (RTR-J1). | § 3.2, § 3.4 |
| **4** | **Sequential-impression discipline.** On `placementSessionId` re-mint, defer constructing the new AdSession-equivalent + first emission until post-mint `onReady({protocolNonce})` resolves (RTR-D21 analogue). | The OMID extension does **not** construct a new AdSession or emit `setClientInfo`/`sessionStart` for a re-minted session until its `onReady` re-fires with the new OMID `protocolNonce` (router § 5.3 / RTR-D21). This is **wired to #240**: the sanctioned `placementSessionId` re-mint API (which calls `rederiveAllProtocolNonces()` and gates new-iframe wiring behind its `await`) is the only path that mutates `placementSessionId`; the immutability guard lands with this work. The OMID extension is the **first real consumer** of per-impression re-derivation. | § 9 (this row), #240 |
| **5** | **Cross-vendor isolation transport.** Shim→vendor delivery is direct same-realm callback (canonical); per-vendor `MessageChannel` for the nested-iframe edge; **forbidden:** `postMessage` broadcast. | Shim invokes each observer's callback directly in the creative-iframe JS context (§ 7.2). In-iframe `postMessage` broadcast is explicitly forbidden. Per-vendor `MessageChannel` noted for the uncommon nested-iframe vendor. This is OMID's residue — the router can't help inside the iframe (§ 7.2). | § 7.2 |
| **6** | **Protocol nonce never reaches vendor JS.** `protocolNonce` is operator-private transport authenticator; must not appear in any payload, return value, or observer callback. Pin with a test. | The shim strips `sharcNonce`/`placementSessionId`/`sequence` transport fields before invoking observer callbacks; the observer event is exactly `{adSessionId, timestamp, type, data}` (§ 5.2). The nonce is not on the `omid3p` surface (§ 5.1). § 10 pins a test asserting no observer callback, return value, or `data` payload contains the OMID `protocolNonce`. | § 5.1, § 5.2, § 10 |
| **7** | **Response correlation.** GUID-keyed `callbackMap` inside the OMID extension handler for request/response (e.g. `registerSessionObserver` ack). Mirrors OMID's own internal architecture. | Each `registerSessionObserver` / `addEventListener` mints a GUID → `callbackMap[guid] = callback`, sent as `subscription.subscriptionId`; publisher responses/`Unregister` acks carry the GUID so the shim resolves the right local callback (§ 5.5). | § 5.5 |

All seven are router-neutral (router § 16.2: "zero router-side adjustments needed") and live one layer up — in the OMID extension and the iframe shim. This design owns all seven.

---

## 10. Test Strategy (producer-side only)

> **Scope boundary.** This design is the **producer**. The Layer-2 validator integration — loading **real** DV/IAS/Moat/Integral scripts against the corpus and asserting `omid3p` end-to-end — is **#244 (Codex)**, an independent verifier. Do **not** design or build the validator here. The tests below are producer-side fixtures only.

### 10.1 Two-layer model (per #217 lock)

- **Layer 1 (this design, unit/CI):** stub OMID Verification Client embedded in a synthetic HTML creative. Asserts `omid3p` present, observer registration succeeds, `sessionStart`/`loaded`/`impression` callbacks fire.
- **Layer 2 (#244, separate):** Codex's validator loads real vendor scripts against the corpus. Not in scope here.

### 10.2 Layer 1 — stub Verification Client fixture

A synthetic creative (`test/browser/test-omid-shim-fixture.html`) with an inline stub mirroring IAB `verification-client.js`'s detection pattern: probe `window.omid3p`, `registerSessionObserver`, push captured events to `window.__capturedOmidEvents`.

| Assertion | Expected |
|-----------|----------|
| `__capturedOmidEvents` contains `'sessionStart'` after container reaches `omid-active` | true |
| `'loaded'` follows `'sessionStart'` | true |
| `'impression'` follows `'loaded'` | true |
| `'sessionFinish'` after container teardown | true |
| No `'__no_omid3p__'` entry | true |
| `'impression'` appears at most once | true |
| `window.omid3p` is `undefined` after `'sessionFinish'` | true |
| **No observer-event `data` / callback arg contains the OMID `protocolNonce`** (§ 9 dep 6) | true |

Stub Service Script / Session Client (`test/browser/stubs/omid-service-stub.js`, `omid-session-client-stub.js`) implement only the surface `OmidCompatBridge._createSession()` touches (`Partner`, `Context`, `AdSession.start()`, `AdSession.registerSessionObserver`, `AdEvents.loaded()`, `AdEvents.impressionOccurred()`, `AdSession.finish()`).

### 10.3 Layer 1 — router-consumption + bridge-only unit tests

| Test | Description |
|------|-------------|
| OMID registers `prefix:'SHARC:Omid:'` with declared `types`/`phases`; `onReady` delivers a `protocolNonce` distinct from the renderer's | jsdom + router |
| Router gate silent-drops inbound `SHARC:Omid:Register` with bad nonce / bad placementSessionId / bad source (router owns this — assert via router) | jsdom + router |
| Out-of-phase `SHARC:Omid:Event` (e.g. arriving in `attaching-renderer`) raises `unauthorized_protocol` | jsdom + router |
| Shim install in clean iframe exposes `window.omid3p` with two functions | jsdom |
| Shim queues subscriptions registered before `sessionStart`; materializes on session start | jsdom |
| Shim replays the last `sessionStart` to a late observer (cap 1) | jsdom |
| Shim caps subscriptions at `MAX_OMID_SUBSCRIPTIONS` | jsdom |
| Shim-side inbound validator rejects bad source / origin / nonce / placementSessionId (§ 3.5 / OMID-Q3) | jsdom + mock postMessage |
| Cross-vendor: two observers both receive `sessionStart` via direct callback; no in-iframe `postMessage` broadcast occurs (§ 7.2) | jsdom |
| **OMID `protocolNonce` never appears in any observer callback / return value / `data`** (§ 9 dep 6) | jsdom |
| GUID `callbackMap`: a registration's ack resolves the correct local callback (§ 5.5 / dep 7) | jsdom |

### 10.4 Coverage gates

- `npm run test` (jsdom) passes with new shim + router-consumption tests.
- `npm run test:browser` (Puppeteer) passes with the new fixture.
- Existing `OmidCompatBridge` + router tests continue to pass — no regressions on publisher-page lifecycle or renderer protocol.

---

## 11. Migration Plan

### 11.1 Pre-1.0 clean break

Per `feedback_pre_1_breaking_changes`: no deprecation period, no aliases, no migration warnings. Breaking changes ship clean in 0.7.8.

### 11.2 Behavior matrix

| Configuration | Before | 0.7.8 | Breaking? |
|---------------|--------|-------|-----------|
| `OmidCompatBridge` configured, default options, Markup variant | OM SDK on publisher page; container-only OMID; no `omid3p` in iframe | + `window.omid3p` exposed in creative iframe; events relayed via router `SHARC:Omid:` protocol | Yes — new iframe surface; creative `window.omid3p` collisions throw (§ 11.3) |
| Creative URL variant **without** `useMarkupInjection` | Same | OM SDK + container OMID lifecycle run; `com.iabtechlab.sharc.omid` **not** advertised (shim un-installable) | Yes — feature conditional on installable variant |
| `injectScripts` / `injectIntoMarkup` callers | No-op identity | Identity when shim-install not configured; prepends shim `<script>` when configured | Yes — no longer strict identity |
| Operators reading `options.verificationScripts` | Field carries OMID delivery URLs | Same field/shape; docs reframe as supplementary | No code break, doc-only |

### 11.3 Throw-on-legacy enforcement

The 0.7.3 lock removed the creative-side OMID *message* path (`SHARC:Creative:requestOmid`) — that stays removed. 0.7.8 adds a `window.omid3p` install instead. If the creative attempted to install its own `window.omid3p`, the shim throws synchronously:

```javascript
// In sharc-omid-shim.js
if (window.omid3p) {
  throw new Error(
    '[SHARC OMID Shim] window.omid3p is already installed. ' +
    'SHARC manages omid3p exposure; nested or duplicate installations are not supported.'
  );
}
```

Surfaces collisions immediately rather than silently winning a race (Decision OMID-D10, carried).

### 11.4 CHANGELOG entry (proposed)

```markdown
## [0.7.8] — 2026-MM-DD

### Added
- `OmidCompatBridge` now exposes `window.omid3p` to the creative iframe per
  IAB OMID API v1.5, enabling OMID-aware verification scripts that arrive
  inline in `adm` (DoubleVerify, IAS, Moat, Integral) to detect SHARC's OMID
  integration and register session observers. The container-owned OM SDK
  lifecycle (0.7.3) is unchanged — the publisher-page `AdSession` is the
  single source of truth; the iframe shim is a one-way relay.
- New `sharc-omid-shim.js` artifact, installed before `document.write`.
  Exposes only the two IAB-required methods.
- OMID is now the SHARC Protocol Router's second consumer (prefix
  `SHARC:Omid:`); publisher↔iframe transport inherits the router's gating
  and per-protocol nonce derivation (0.7.7). No bespoke OMID envelope-gating.

### Changed
- `OmidCompatBridge.options.verificationScripts` semantics narrowed: now a
  **supplementary** publisher / wrapper / vendor injection path, no longer
  the canonical OMID delivery channel for HTML. Field name and shape
  unchanged. Becomes canonical for future Native (eventtrackers event=555)
  and VAST (<AdVerifications>) creative formats.

### Breaking
- Creative URL variant **without** `useMarkupInjection` no longer advertises
  `com.iabtechlab.sharc.omid`. The publisher-page `AdSession` still runs for
  `options.verificationScripts` entries, but inline `omid3p`-probing scripts
  won't find `omid3p`. Enable `useMarkupInjection: true` to opt in.
- Creatives that installed their own `window.omid3p` (a no-op under prior
  releases) now throw at SHARC shim install time. No deprecation period.
```

---

## 12. Implementation Plan

> Design-phase artifact. No production code in this doc; the table is sequencing guidance for the develop phase.

### 12.1 New files

| File | Purpose | ~LOC |
|------|---------|------|
| `src/sharc-omid-shim.js` | Iframe-side: installs `window.omid3p`, shim-side inbound validator, subscription registry, GUID `callbackMap`, cross-vendor direct dispatch | ~280 |
| `test/browser/stubs/omid-service-stub.js` | Minimal stub OM SDK Service Script | ~80 |
| `test/browser/stubs/omid-session-client-stub.js` | Minimal stub Session Client | ~100 |
| `test/browser/test-omid-shim-fixture.html` | Synthetic creative with inline Verification Client stub | ~60 |
| `test/browser/test-omid-shim-puppeteer.js` | Puppeteer harness | ~200 |
| `test/node/test-omid-shim.js` | jsdom shim unit tests | ~300 |
| `test/node/test-omid-router-consumption.js` | jsdom tests for router registration + gate behavior + nonce-isolation | ~280 |

### 12.2 Modified files

| File | Change |
|------|--------|
| `src/sharc-omid-bridge.js` | Add `onContainerLifecycleEvent` hook that registers `SHARC:Omid:` with the router on `load`; relay AdSession events via `buildOutbound`; drive `omid-active`/`omid-finishing` transitions (via OMID-Q2-resolved path); replace `injectScripts`/`injectIntoMarkup` no-ops with shim-injection + trusted-nonce-injection when opt-in is set. **Remove** any bespoke `window.addEventListener('message')` / hand-rolled envelope validator. |
| `src/sharc-container.js` | Per #240: make `placementSessionId` structurally immutable + sanctioned re-mint API that calls `rederiveAllProtocolNonces()` (lands with this work, RTR-D21). Possibly a narrow container-internal signal for OMID phase transitions (OMID-Q2 resolution b). No router-API change. |
| `examples/renderer/index.html` | Add `omid` boolean accept path on `SHARC:Renderer:render`; load `sharc-omid-shim.js` from the same same-origin allowlist as existing bridge modules; source-rewrite the OMID nonce (§ 4.3 mechanism i). |
| `rollup.config.js` | Add `sharc-omid-shim.js` as a build entry → `dist/`. |
| `package.json` | Add `npm run test:omid-shim` script. |
| `docs/api-reference.md` / in-source JSDoc | Document the `omid3p` surface and the opt-in option. |
| `CHANGELOG.md` | Entry per § 11.4. |

**No `src/sharc-protocol-router.js` changes.** OMID is a pure consumer (router § 16.2).

### 12.3 Constructor option for opt-in shim install

```javascript
new OmidCompatBridge({
  omSdkServiceScriptUrl: '...',
  omSdkSessionClientUrl: '...',
  verificationScripts: [...],  // supplementary, per § 8
  exposeOmid3p: true,          // NEW — install window.omid3p shim. Default: true.
});
```

> **Open question OMID-Q6 (default for `exposeOmid3p`).** Argument for `true`: spec-compliance is the intended behavior; the corpus's 24% inline-OMID rate means `false` leaves measurement on the table; conflicts loud-fail at shim install (§ 11.3), not silent-corrupt. Argument for `false`: minimize blast radius for a 0.7.x bump. **Architect recommendation: `true`.** Product decision — Jeffrey's call (carried from parked D2).

### 12.4 Step ordering

| Step | Description |
|------|-------------|
| 1 | `sharc-omid-shim.js` standalone — `omid3p` surface, subscription registry, GUID `callbackMap`, cross-vendor direct dispatch (no transport) |
| 2 | Shim transport: inbound `SHARC:Omid:Event` listener + shim-side validator; outbound `SHARC:Omid:Register` poster signed with injected nonce |
| 3 | `OmidCompatBridge` router registration (`onContainerLifecycleEvent`); relay via `buildOutbound`; `omid-active`/`omid-finishing` transitions |
| 4 | Trusted nonce injection (§ 4.3): renderer source-rewrite (Markup) + MessageChannel (URL+srcdoc) |
| 5 | `#240`: `placementSessionId` immutability + sanctioned re-mint API + sequential-impression deferral (dep 4) |
| 6 | Shim install via `injectIntoMarkup` (URL+markup) + renderer `omid` flag (Markup) |
| 7 | Layer 1 unit tests (§ 10.3) |
| 8 | Layer 1 fixture + Puppeteer (§ 10.2) |
| 9 | CHANGELOG, JSDoc, narrow `options.verificationScripts` framing (§ 8) |
| 10 | Code Reviewer + Security Engineer pass (esp. OMID-Q2, OMID-Q3, OMID-D9) |

---

## 13. Open Questions & Design Risks

Consolidated for the reviewer + Jeffrey's lock. None are guesses about IAB spec behavior — where the spec is silent, it is flagged as such.

| ID | Question / risk | Recommendation | Needs |
|----|------------------|----------------|-------|
| **OMID-Q1** | `Register` phase membership depends on whether the shim posts `SHARC:Omid:Register` at queue time (pre-`omid-active`) or defers until `sessionStart`. Coupled to § 5.5 queueing policy. | Lock § 5.5 first; prefer the narrower `omid-active`-only gate if the shim defers. | Design lock |
| **OMID-Q2** | OMID needs `omid-active`/`omid-finishing` phase transitions, but RTR-D14 keeps `transitionTo` container-internal (not extension-exposed). | (b) Container drives the transition off an OMID-internal lifecycle signal — preserves the router's "phases are container-controlled" trust anchor. | **Security Engineer review** + Jeffrey lock |
| **OMID-Q3** | Shim-side inbound validator (4 checks) is OMID-specific code, not the router (router is publisher-page-only). Drift risk vs router anchors. | Accept; pin with test (§ 10.3). Shipping a router into the hostile iframe is strictly worse. | Code Reviewer pass |
| **OMID-Q4** | Does renderer source-rewrite nonce injection (§ 4.3-i) generalize to the URL+`srcdoc` variant (no renderer)? | Likely no — use MessageChannel (§ 4.3-ii) for URL variant. Confirm during implementation; don't assume. | Implementation spike |
| **OMID-Q5** | Replay-most-recent-`sessionStart` to late observers is **beyond the OMID spec's letter** (spec describes the API, not late-registrant ordering). | Recommend as durable default; cap 1. | Jeffrey lock |
| **OMID-Q6** | Default for `exposeOmid3p` (`true` vs `false`). | `true` (spec-compliant intent; loud-fail on conflict). | Jeffrey (product) |
| **OMID-Q7** | `SHARC:Omid:Unregister` in scope for 0.7.8? IAB Verification Client exposes **no** unregister today, but the spec does not forbid one. | In-scope: small surface, debuggability win; declared in `types` already (§ 3.2). | Jeffrey lock |
| **Risk R1** | OMID v1.5 `sessionFinish` ~1.0s grace window: if the container terminates hard (`_terminate()` → `transitionTo('terminated')`) before the grace elapses, the final `sessionFinish` `Event` is out-of-phase (`terminated` not in `Event`'s `phases`) and silent-dropped. | `omid-finishing` exists to cover the grace; ensure teardown enters `omid-finishing` and flushes `sessionFinish` **before** `terminated`. Pin a test. | Implementation + test |
| **Risk R2** | The IAB spec does not define a cross-frame transport (same-realm only). SHARC's nonce solves a SHARC transport problem; if a future OMID spec revision *does* define cross-frame semantics, SHARC's transport may need re-alignment. | Documented as SHARC-specific (§ 1); revisit if OMID spec adds cross-frame. | Awareness |

### 13.1 Does the locked #217 decision need revisiting?

**No.** The #217 lock (omid3p primary path; `verificationScripts` supplementary; consume the router) holds cleanly under the router. One nuance worth surfacing to Jeffrey: #217's acceptance criterion says events "propagate via a SHARC Protocol Router protocol (prefix `SHARC:Omid:`), with envelope types and lifecycle phases declared" — this design satisfies that literally (§ 3.2, § 3.4), **but** OMID-Q2 reveals a tension between "OMID drives phase transitions" and the router's RTR-D14 ("phases are container-controlled, `transitionTo` not extension-exposed"). The resolution (container drives transitions off an OMID signal) is a small architectural detail #217 didn't anticipate. It does not contradict the lock — it refines *how* OMID reaches the reserved phases. Flagged here so Jeffrey sees it at lock time.

---

## 14. Decision Log

| ID | Decision | Status | Rationale |
|----|----------|--------|-----------|
| **OMID-D1** | OMID transport rides the router's `window.message` channel (`SHARC:Omid:`), not a standalone `MessageChannel`; `MessageChannel` reserved only for one-time trusted nonce injection | ✅ Locked (this revision) | § 3.1. Keeps the router gate authoritative for all steady-state OMID traffic; avoids reintroducing bespoke gating. |
| **OMID-D2** | Trusted nonce injection: renderer source-rewrite (Markup) + MessageChannel (URL+srcdoc) | 🟡 Recommend | § 4.3. Both keep nonce off `location.hash`. URL-variant generalization is OMID-Q4. |
| **OMID-D3** | Exposure timing: before `document.write(creativeHtml)`, same point as MRAID/SafeFrame | ✅ Locked (carried) | § 4.1. Matches `verification-client.js` synchronous `omid3p` probe. |
| **OMID-D4** | `'omid'` stays out of `KNOWN_BRIDGE_IDENTIFIERS`; renderer accepts separate `omid` flag | ✅ Locked (carried) | § 4.2. OMID is not API translation. |
| **OMID-D5** | `omid3p` event vocabulary is faithful relay; no SHARC-specific extensions; transport fields stripped before observer delivery | ✅ Locked (carried) | § 7.2. Shim is not an information channel. |
| **OMID-D6** | `MAX_OMID_SUBSCRIPTIONS = 64` | 🟡 Recommend lock | § 7.3. Conservative; tunable via option. |
| **OMID-D7** | OMID is a router **consumer** — registers `SHARC:Omid:`, adds **zero** router-side code | ✅ Locked (this revision) | § 2.3, § 3.1. Router § 16.2. |
| **OMID-D8** | Per-protocol OMID nonce is router-derived (HMAC salted with `placementSessionId`), delivered via `onReady`; OMID does **not** derive its own nonce and does **not** reuse the renderer nonce | ✅ Locked (this revision) | § 3.2, § 7.1. Resolves parked B1/B2. |
| **OMID-D9** | Shim-side inbound validator: best-effort origin + load-bearing `event.source === window.parent` for srcdoc/opaque-origin | 🟡 Recommend (security review) | § 7.4. Router gate (publisher side) is opaque-origin-safe via contentWindow identity; this is narrower than the parked framing. |
| **OMID-D10** | Throw synchronously at shim install if `window.omid3p` already defined | ✅ Locked (carried) | § 11.3. |
| **OMID-D11** | OMID feature not advertised in Creative URL variant without `useMarkupInjection` | ✅ Locked (carried) | § 4.2. Honest behavior. |
| **OMID-D12** | `options.verificationScripts` field name/shape unchanged; semantics narrowed (supplementary; canonical for future Native/VAST) in doc only | ✅ Locked (per #217) | § 8. |
| **OMID-D13** | Event types: `sessionStart`/`sessionFinish`/`sessionError`/`loaded`/`impression`/`geometryChange` (video deferred) | ✅ Locked (carried) | § 5.3. |
| **OMID-D14** | Late-registered observers get a replay of the most recent `sessionStart` (cap 1) | 🟡 Recommend lock (beyond spec letter) | § 5.4 / OMID-Q5. |
| **OMID-D15** | Iframe shim unidirectional for events (`Event` outbound only); registration handshake bidirectional (`Register`/`Unregister` inbound) | ✅ Locked (carried) | § 6.3. IAB `omid3p` is observer-only. |
| **OMID-D16** | Validator integration (real DV/IAS/Moat) is **#244 (Codex)**, out of scope here | ✅ Locked (per #217) | § 10. Producer/verifier split. |
| **OMID-D17** | 0.7.3 D7/D17 reversal: `window.omid3p` IS exposed in the creative iframe | ✅ Locked (per #217) | The 0.7.3 decision was correct against the SHARC creative API surface but missed the inline-verification-script path. |
| **OMID-D18** | `placementSessionId` immutability + sanctioned re-mint API lands **with** this work (#240); OMID is the first consumer of per-impression re-derivation | ✅ Locked (per #240) | § 9 dep 4. RTR-D21 tripwire. |
| **OMID-D19** | Phase transitions reach the router's **reserved** `omid-active`/`omid-finishing` via a container-internal path, not extension-exposed `transitionTo` | 🟡 Recommend (OMID-Q2, security review) | § 3.4. Preserves RTR-D14's "phases are container-controlled." |

### Items requiring Jeffrey's explicit input

| ID | Item | Why |
|----|------|-----|
| **OMID-Q6 / D6-default** | Default for `exposeOmid3p` (`true` vs `false`) | Product decision on default opt-in; 24% corpus OMID-inline rate vs blast radius. |
| **OMID-D6** | Cap value (64) | Field intelligence may justify different. |
| **OMID-Q5 / D14** | Replay policy for late observers | Beyond IAB spec letter — durable-commitment call. |
| **OMID-Q7** | `SHARC:Omid:Unregister` in/out for 0.7.8 | Marginal surface vs debuggability. |
| **OMID-Q2 / D19** | How OMID reaches reserved phases without extension-exposed `transitionTo` | Touches the router's phase trust anchor — Security Engineer + Jeffrey lock. |

---

*End of design — REVISED draft awaiting design review + Jeffrey's lock.*
